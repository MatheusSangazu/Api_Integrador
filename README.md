É preciso cirar uma pasta Config dentro da Pasta SRC com um arquivo "config.js" 

Dentro dele deve conter basicamente suas credenciais de api

Exemplo:

module.exports = {
    clientId: 'ClienteID',
    clientSecret: 'ClienteSecret',
    clinicToken: 'Token',
    apiBaseUrl: 'URL BASE' 
  };
